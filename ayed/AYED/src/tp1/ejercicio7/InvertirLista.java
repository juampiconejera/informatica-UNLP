package tp1.ejercicio7;

import java.util.*;

public class InvertirLista {
	public void invertirArrayList(ArrayList<Integer> lista) {
		List<Integer> listaInversa = new ArrayList<>();
		int index = lista.size()-1;
		recursividad(lista, listaInversa, index);
	}
	
	private void recursividad(ArrayList<Integer> lista, List<Integer> listaInversa, int index) {
		if (index >= 1) {
			
		}
	}
}
